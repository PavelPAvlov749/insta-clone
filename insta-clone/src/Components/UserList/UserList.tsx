import React from "react";
import { useSelector } from "react-redux";
import { Global_state_type } from "../../Redux/Store";
import { UserType } from "../../Redux/Types";

export const UserList : React.FC = React.memo((props) => {

    return (
        <section>

        </section>
    )
})