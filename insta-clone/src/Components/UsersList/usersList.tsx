import React from "react";
import styles from "../../Media/usersList.module.css"

export const UsersList : React.FC = React.memo((props) => {

    
    return (
        <section className={styles.UsersList}>

        </section>
    )
})